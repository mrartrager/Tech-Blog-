const {Model, DataTypes} = require(sequelize);
// const { User } = require('.');
// const { Post } = require('.');
const sequelize = require('../config/connection');

User.init({
    id: {
        
    }
})

module.exports=User;